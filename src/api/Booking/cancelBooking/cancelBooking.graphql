type Mutation {
    cancelBooking(
        ownerId: String!
        bookingId: String!
        refundPrice: String!
        contact: String!
        fullName: String!
        prices: [InputPrice!]!
    ): Booking!
}