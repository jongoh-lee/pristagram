input CreatePrice {
    dateString: String!
    priceState: String!
    self: Boolean!
}

input DeletePrice {
    id: String!
}

input UpdatePrice {
    id: String!
    dateString: String!
    priceState: String!
    self: Boolean!
}   

type Mutation {
    editCalendar(
        updatePrice:[UpdatePrice]!
        createPrice:[CreatePrice]!
        deletePrice:[DeletePrice]!
    ):Owner!
}