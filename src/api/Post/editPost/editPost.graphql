enum ACTIONS {
  EDIT
  DELETE
}

input EditFile {
  id: String!
  url: String!
}

input DeleteFile {
  id: String!
}

type Mutation {
  editPost(
    id: String!
    tasting: String
    action: ACTIONS!
  ): Post!
}